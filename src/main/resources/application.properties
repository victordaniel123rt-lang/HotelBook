spring.application.name=HotelBook
server.port: 9091
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:xe
spring.datasource.username=root2
spring.datasource.password=1469371
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Configuraciones de JPA / Hibernate para Oracle
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.globally_quoted_identifiers=true
spring.jpa.show-sql=true
# Se utiliza el dialecto genérico de Oracle que detecta automáticamente la versión (Oracle 11g, 12c, 19c, 23c, etc.)
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect

# Configuración del Pool de Conexiones (Hikari)
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=5

# Logs de depuración para SQL
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE